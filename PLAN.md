# Flux 多端 RSS 阅读器工作计划

## 1. 技术选型与架构

### 1.1 语言与框架

- **语言**：Dart
- **UI 框架**：Flutter 3.x
- **目标平台**：
  - 首发：Windows / macOS / Linux、Android / iOS
  - 后续：Web（PWA，视媒体兼容性再开放）

选择 Flutter 的理由：
- 单一代码库覆盖桌面 + 移动多端，减少重复开发。
- 列表虚拟化、原生渲染、硬件加速支持好，适合“大量图片/视频不卡不闪退”的要求。
- 生态中有成熟 RSS/Atom 解析、SQLite、缓存、视频播放等方案。
- 主题系统灵活，可完整实现简洁现代视觉风格。

### 1.2 整体架构

采用分层 + 模块化架构，严格分离 UI、业务、数据、媒体。

```
┌──────────────────────────────────────────┐
│ Presentation Layer（Flutter Widgets）     │
│  - 订阅列表 / 文章列表 / 阅读器 / 设置     │
├──────────────────────────────────────────┤
│ State Management（Riverpod）              │
│  - 订阅状态、文章状态、媒体状态、设置状态  │
├──────────────────────────────────────────┤
│ Domain Layer（实体 + 用例）                │
│  - Feed 实体、Article 实体、解析用例、过滤 │
├──────────────────────────────────────────┤
│ Data Layer                               │
│  - RSS/Atom 解析器（Isolate 中运行）       │
│  - Drift/SQLite 本地持久化                │
│  - Dio 网络层；OPML 导入/导出             │
│  - 媒体缓存与懒加载引擎                    │
└──────────────────────────────────────────┘
```

关键组件：
- **State Management**：Riverpod
- **数据库**：Drift（SQLite），移动端与桌面端统一
- **RSS/Atom 解析**：基于 `webfeed` 或 `feed_blocks`，包裹成独立解析服务
- **网络**：Dio + 超时/重试/限流
- **图片缓存**：自研“尺寸感知 + LRU + 磁盘缓存”媒体引擎，避免一次性解码大图
- **视频播放**：桌面用 `media_kit`，移动端用 `video_player`/系统播放器；全部“按需加载、点击播放”

## 2. 流畅度与稳定性设计（核心要求）

1. **列表虚拟化**
   - 使用 `ListView.builder` / `SliverList`，只构建可视区域内的条目。
2. **媒体懒加载**
   - 图片仅在其进入可视区预加载范围时才解码；
   - 缩略图按实际展示尺寸解码，不加载原图大尺寸；
   - 视频在列表中只显示封面，点击后才初始化播放器。
3. **解析与 IO 隔离**
   - RSS 抓取、XML 解析、数据库写入都在 Isolate / 后台线程完成，不阻塞 UI。
4. **内存有界**
   - 图片/视频缓存设置容量上限（如内存 64MB、磁盘 512MB）；
   - 监听内存压力事件，主动淘汰旧缓存；页面销毁时释放播放器。
5. **逐 Feed 错误隔离**
   - 单个订阅源超时、解析失败、图片损坏不影响其他源和应用本体。
6. **播放器生命周期管理**
   - 切换到后台、离开详情页、列表滚动时暂停/释放视频，避免内存堆积。
7. **崩溃兜底**
   - 全局异常捕获 + 恢复现场；严重媒体错误单独降级为纯文本阅读。

## 3. 功能计划

### 3.1 MVP（首个可审阅版本）
- [ ] 订阅源管理：添加 / 编辑 / 删除 / 分组 / 排序
- [ ] 文章列表：虚拟化列表、无图/缩略图/全文三种视图
- [ ] 阅读器：内置阅读视图、字体大小调节、窄栏/宽栏
- [ ] 已读 / 未读、全部标记已读
- [ ] 收藏 / 稍后读 / 标签
- [ ] 搜索与过滤（关键词、订阅源、时间、已读状态）
- [ ] OPML 导入 / 导出
- [ ] 离线缓存：已抓取文章可离线阅读
- [ ] 媒体引擎：图片缓存、视频点击播放、内存上限保护
- [ ] 简洁现代主题：红 / 黑 / 白几何语言、粗体排版、暗黑模式
- [ ] 桌面快捷键（j/k 下一条、m 已读切换、r 刷新等）
- [ ] 移动端手势（滑动返回、长按操作）
- [ ] 设置页：刷新间隔、缓存上限、默认视图、主题切换

### 3.2 第二阶段候选（审阅后决定是否纳入）
- [ ] 后台刷新与系统通知（新文章提醒）
- [ ] 全文抓取（Readability 风格正文提取）
- [ ] 多端同步（WebDAV / Fever / 自托管 API，可选）
- [ ] Web/PWA 版本
- [ ] 浏览器扩展？暂不纳入

### 3.3 明确不做（第一版）
- 不做自建账号体系
- 不做在线视频站聚合播放，只支持 RSS 中的视频链接/附件
- 不做复杂插件系统

## 4. 视觉风格：简洁现代

- **简洁现代核心元素**
  - 红（#E53935）、黑、白/灰阶为主色
  - 几何切割：斜线、矩形、圆形叠加
  - 粗壮的无衬线字体，标题感强，信息层级清晰
  - 网格化布局，留白克制
- **暗黑模式**
  - 背景深灰/近黑，文字高对比，红色作为强调色
  - 媒体区域在暗色下自动调节亮度/边框，保证可读性
  - 跟随系统或手动切换
- **动效**
  - 克制的位移动效，优先保证滚动流畅

## 5. 里程碑

| 阶段 | 内容 | 完成标志 |
|------|------|----------|
| P0 | 方案确认与计划审阅 | 本文件确认 |
| P1 | 项目脚手架、设计 token、主题系统 | 可在目标平台启动，暗黑主题可见 |
| P2 | 数据层：RSS/Atom 解析、SQLite、OPML | 能添加订阅并列出文章 |
| P3 | 核心 UI：订阅列表、文章列表、阅读器、媒体引擎 | 可完整阅读图文/视频 RSS |
| P4 | 搜索、离线、设置、快捷键、手势、暗黑模式完善 | MVP 功能全部可用 |
| P5 | 性能压测、内存排查、崩溃修复、多端打包 | 发布首个内部测试版 |

## 6. Subagent 协作计划（最多 5 个）

1. **设计系统 Subagent**：产出简洁现代设计 token、组件规范、暗黑模式调色板。
2. **数据层 Subagent**：实现 RSS/Atom 解析、数据库 schema、OPML 导入导出。
3. **媒体引擎 Subagent**：实现图片懒加载/缓存/内存上限、视频播放器生命周期。
4. **核心 UI Subagent**：实现订阅列表、文章列表、阅读器界面与交互。
5. **测试与性能 Subagent**：多端压测、内存泄漏检查、卡顿/崩溃复现与修复。

使用原则：每个 subagent 负责独立模块，产出后由主线程统一集成审阅；不超过 5 个并行或累计任务。

## 7. 交付物

- `PLAN.md`：本计划
- `docs/design.md`：设计规范与主题 token
- `docs/architecture.md`：模块边界与数据流
- Flutter 工程源码
- 多端安装包（后续）
- 测试报告 / 性能报告

## 8. 当前进度（第一轮实现）

- [x] 确认技术选型：Flutter + Riverpod + SQLite
- [x] 确认首发：桌面优先，后续移动端
- [x] Flutter 工程骨架（Windows/macOS/Linux/Android/iOS）
- [x] 简洁现代主题 + 暗黑模式
- [x] RSS/Atom 解析
- [x] SQLite 数据层
- [x] 订阅添加/删除/刷新
- [x] 文章列表/阅读器/已读/收藏/稍后读/搜索
- [x] 图片懒加载与按尺寸解码
- [x] 视频占位 + 外部播放器
- [x] OPML 导入/导出
- [x] 设置页（主题、缩略图、字号、缓存上限）
- [x] 桌面快捷键（j/k 选择、r 刷新、m 全部已读、/ 搜索、n 添加、Enter 打开）
- [x] 全文抓取（Readability 风格，正文过短时一键抓取原文全文）
- [x] 富文本阅读：HTML 块级渲染、Markdown 行内语法、KaTeX 风格公式
- [x] 图片按 0.9 幅宽展示
- [x] 内嵌视频播放器（media_kit，失败自动回退外部播放器）
- [x] 后台定时刷新 + 新文章本地通知
- [x] 移动端响应式布局（Drawer + NavigationBar）与长按手势
- [x] 存储管理：文本 30 天、图片 7 天、视频 1 天自动清理
- [x] 媒体缓存 LRU：超过上限自动清理最早加载内容
- [x] 设置页分类与可调存储/缓存参数（注意：媒体缓存空间 MB 上限被移除，改为按文件数上限控制）
- [ ] 多端同步（后续）

## 8.1 代码审查后已知偏差（2025 年清理后更新）

以下为原计划中“看起来已完成/应做到”但实际未落地或打折扣的能力，排期时需优先补齐：

- [ ] **解析与 IO 隔离（§2.3）**：RSS/XML/HTML 解析仍在 UI isolate，订阅多时刷新会卡顿；计划要求放入 Isolate。
- [ ] **网络重试/限流（§3.1 数据层）**：Dio 只有超时，没有重试与限流。
- [ ] **内存有界（§2.4）**：`mediaCacheLimitMb` 设置已移除（flutter_cache_manager 不支持按字节容量上限）；目前只有最大文件数 LRU。
- [ ] **崩溃兜底（§2.7）**：未实现全局异常捕获与恢复现场，媒体错误降级仅部分覆盖。
- [ ] **文章分页**：`getArticles` 默认限制 1000 条，`loadMore()` 已提供但 UI 尚未接线，列表仍会静默截断。
- [ ] **搜索性能**：已加 300ms 防抖，但仍为 `LIKE '%…%'` 全表扫描；数据量大时建议上 FTS5。
- [ ] **订阅刷新并发**：`refreshAll` 已改为每批 4 个并发，但未做失败重试。
- [ ] **OPML 分组往返**：导入/导出已支持分组结构；旧版扁平 OPML 仍可兼容导入。
- [ ] **测试覆盖**：已有 parser/RSSHub/数据库/OPML 测试；FeedController 与 widget 测试仍缺失。

## 9. 待确认事项

- [x] 首发平台：桌面优先（已确认）
- [x] 全文抓取（Readability）纳入 MVP（已实现）
- [x] 视频播放采用内嵌播放器（已实现，失败回退外部）
- [ ] 是否需要第一版就支持多端同步？